import * as Mixins from './mixins';
import * as Types from './types';

/**
 * Phaser 3 TypeScript utitlities.
 */
const PhaserTSUtils = { Mixins, Types };
global['PhaserTSUtils'] = PhaserTSUtils;
export default PhaserTSUtils;
